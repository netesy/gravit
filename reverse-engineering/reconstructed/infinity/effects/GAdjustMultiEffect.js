/**
 * GAdjustMultiEffect
 * 
 * Part of the Gravit Designer core engine
 * Reconstructed from minified source
 */

function GAdjustMultiEffect() {
        n.call(this);
      }

// Prototype methods
GAdjustMultiEffect.prototype.getNodeNameTranslated = function () {
          return l.getValue("GAdjustMultiEffect", "name", this.getNodeName());
        }

GAdjustMultiEffect.prototype.toString = function () {
          return "[Object GAdjustMultiEffect]";
        }

module.exports = GAdjustMultiEffect;
