/**
 * GApplicationStatusEvent
 * 
 * Part of the Gravit Designer core engine
 * Reconstructed from minified source
 */

function GApplicationStatusEvent(e, t) {
      (this.type = e), (this.sidebar = t);
    }

// Prototype methods
GApplicationStatusEvent.prototype.toString = function () {
        return "[Object GSidebars.SidebarEvent]";
      }



module.exports = GApplicationStatusEvent;
