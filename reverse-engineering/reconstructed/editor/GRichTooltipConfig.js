/**
 * GRichTooltipConfig
 * 
 * Part of the Gravit Designer core engine
 * Reconstructed from minified source
 */

function GRichTooltipConfig(e, t, n) {
      (this.type = e), (this.document = t), n && (this.data = n);
    }

// Prototype methods
GRichTooltipConfig.prototype.toString = function () {
        return "[Object GDocumentEvent]";
      }


module.exports = GRichTooltipConfig;
